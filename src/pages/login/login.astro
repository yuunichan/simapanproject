---
// SI MAPAN - Sistem ruMAh tangga dan kearsiPAN
// Login Page Component

import '../../styles/global.css'
---


<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SI MAPAN - Login</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600&family=Playfair+Display:wght@700&display=swap"
      rel="stylesheet"
    />    
  </head>

  <body>
    <div class="card">
      <!-- Logo -->
      <div class="logo-area">
        <div class="logo-badge">
          <span class="logo-text">
            <span class="si">SI </span><span class="mapan">MAPAN</span>
          </span>
        </div>
        <p class="logo-tagline">
          Sistem ru<strong>MAh</strong> tangga dan kearsi<strong>PAN</strong>
        </p>
      </div>

      <!-- Step Indicator -->
      <div class="step-dots">
        <div class="dot active"></div>
        <div class="dot"></div>
        <div class="dot"></div>
      </div>

      <!-- Role / Jabatan -->
      <div class="form-group">
        <label class="form-label" for="role">Role / Jabatan</label>
        <div class="select-wrapper">
          <select id="role" name="role">
            <option value="user" selected>User</option>
            <option value="admin">Admin</option>
            <option value="manager">Manager</option>
            <option value="staff">Staff</option>
          </select>
          <span class="select-icon">
            <svg viewBox="0 0 20 20" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
              <path
                fill-rule="evenodd"
                d="M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z"
                clip-rule="evenodd"
              />
            </svg>
          </span>
        </div>
      </div>

      <!-- Divider -->
      <p class="divider">Masuk dengan akun Google</p>

      <!-- Google Login Button -->
      <a href="#" class="btn-google" id="googleLoginBtn">
        <!-- Google "G" logo SVG -->
        <svg class="google-icon" viewBox="0 0 18 18" xmlns="http://www.w3.org/2000/svg">
          <path
            d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844a4.14 4.14 0 01-1.796 2.716v2.259h2.908c1.702-1.567 2.684-3.875 2.684-6.615z"
            fill="#4285F4"
          />
          <path
            d="M9 18c2.43 0 4.467-.806 5.956-2.184l-2.908-2.259c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332A8.997 8.997 0 009 18z"
            fill="#34A853"
          />
          <path
            d="M3.964 10.706A5.41 5.41 0 013.682 9c0-.593.102-1.17.282-1.706V4.962H.957A8.996 8.996 0 000 9c0 1.452.348 2.827.957 4.038l3.007-2.332z"
            fill="#FBBC05"
          />
          <path
            d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0A8.997 8.997 0 00.957 4.962L3.964 7.294C4.672 5.163 6.656 3.58 9 3.58z"
            fill="#EA4335"
          />
        </svg>
        Lanjutkan dengan Google / Gmail
      </a>

    </div>

<script>
  document.getElementById('googleLoginBtn')?.addEventListener('click', (e) => {
    e.preventDefault();
    
    // Mengambil nilai role dari dropdown select
    const role = (document.getElementById('role') as HTMLSelectElement)?.value;
    
    console.log('Login attempt dengan role:', role);

    // Jalankan logika pengecekan role
    if (role === 'admin') {
      // Jika yang dipilih admin, arahkan ke halaman form username & password admin
      window.location.href = `./login/gmail?role=${role}`;
    } else if (role === 'manager') {
      // Jika manager, langsung lempar ke dashboard manager dengan query string
      window.location.href = `/dashboard?role=${role}`;
    }
    else if (role === 'staff') {
      // Jika staff, langsung lempar ke dashboard staff dengan query string
      window.location.href = `./login/gmail?role=${role}`;
    }
    
    else if (role === 'user') {
      // Jika user, langsung lempar ke dashboard user dengan query string
      window.location.href = `./login/gmail?role=${role}`;
    }
    
    else {
      // Jika role lainnya atau belum dipilih
      alert('Silakan pilih role yang valid!');
    }
  });
</script>
  </body>
</html>